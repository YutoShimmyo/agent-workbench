[project]
name = "agent-workbench"
version = "0.1.0"
description = "Personal agent workbench template"
authors = [
    {name = "YutoShimmyo", email = "your.email@example.com"} # TODO: 自身のメールアドレスに更新してください
]
dependencies = []

[project.optional-dependencies]
dev = [
    "pytest",
    "ruff"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
