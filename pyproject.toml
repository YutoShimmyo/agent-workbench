[project]
name = "agent-workbench"
version = "0.1.0"
description = "Personal agent workbench template"
authors = [
    {name = "User", email = "user@example.com"}
]
dependencies = []

[project.optional-dependencies]
dev = [
    "pytest",
    "ruff"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
